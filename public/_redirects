# www -> apex is handled by a Cloudflare Redirect Rule in the zone; Workers static assets only accept relative redirect URLs here.
# Netlify 拖拽上传可能忽略以点开头的目录，这里把 RFC 9116 路径映射到根目录副本
/.well-known/security.txt /security.txt 200
